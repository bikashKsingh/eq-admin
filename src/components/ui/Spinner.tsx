export function Spinner(props: PropsType) {
  return (
    <span
      className="spinner-border spinner-border-sm"
      role="status"
      aria-hidden="true"
    ></span>
  );
}

type PropsType = {};
